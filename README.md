# FrictionReader

controller_dsg.py includes all the functions needed to run the motion stages.
mylistenner.py inludes the ros service for recording the position of stage as well as wrench data from sensors.
stage_position_node.py includes the ros publisher of stage positions.

